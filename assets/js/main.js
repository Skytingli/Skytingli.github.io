const menuButton=document.querySelector('.menu-toggle');const nav=document.querySelector('.site-nav');menuButton.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',open)});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menuButton.setAttribute('aria-expanded','false')}));const panel=document.querySelector('.search-panel');const input=document.querySelector('#site-search');document.querySelector('.search-toggle').addEventListener('click',()=>{panel.hidden=false;input.focus()});document.querySelector('.search-close').addEventListener('click',()=>{panel.hidden=true;input.value='';filterSections('')});function escapeRegExp(s){return s.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')}function filterSections(query){const sections=[...document.querySelectorAll('.searchable')];const q=query.trim().toLowerCase();let shown=0;sections.forEach(section=>{section.querySelectorAll('mark.search-hit').forEach(mark=>mark.replaceWith(document.createTextNode(mark.textContent)));const match=!q||section.textContent.toLowerCase().includes(q);section.classList.toggle('search-hidden',!match);if(match){shown++;if(q){const walker=document.createTreeWalker(section,NodeFilter.SHOW_TEXT);const nodes=[];while(walker.nextNode())nodes.push(walker.currentNode);const regex=new RegExp(`(${escapeRegExp(query.trim())})`,'gi');nodes.forEach(node=>{if(node.parentElement.closest('script,style,mark'))return;if(regex.test(node.nodeValue)){const span=document.createElement('span');span.innerHTML=node.nodeValue.replace(regex,'<mark class="search-hit">$1</mark>');node.replaceWith(...span.childNodes)}})}}});document.querySelector('#no-results').hidden=shown>0}input.addEventListener('input',e=>filterSections(e.target.value));

